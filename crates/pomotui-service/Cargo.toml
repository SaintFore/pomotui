[package]
name = "pomotui-service"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
pomotui-domain = { path = "../pomotui-domain" }
pomotui-platform = { path = "../pomotui-platform" }
pomotui-protocol = { path = "../pomotui-protocol" }
listenfd = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"
chrono = { version = "0.4", default-features = false, features = ["clock"] }

[lints]
workspace = true
